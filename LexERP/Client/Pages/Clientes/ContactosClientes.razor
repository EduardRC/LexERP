@if (Listado == null)
{
    <Cargando />
}
else
{
    <hr />
    <h5>
        <span class="oi oi-people" aria-hidden="true"></span>
        Contactos
        &nbsp; <button class="btn btn-primary" @onclick="@(() => AddItem())"><span class="oi oi-plus" aria-hidden="true"></span></button>
    </h5>

    <ListadoGenerico Listado="Listado">
        <NoHayRegistros>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Cargo</th>
                        <th>Nombre</th>
                    </tr>
                </thead>
            </table>
            <br />
            <text>No hay registros para mostrar</text>
        </NoHayRegistros>
        <HayRegistros>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Cargo</th>
                        <th>Nombre</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Listado)
                        {
                        <tr @onclick="@(() => EditarDetalle(item))" style="cursor:pointer;">
                            <td>@item.Cargo</td>
                            <td>@item.Nombre</td>
                            <td><a @onclick="@(() => Eliminar(item))" @onclick:stopPropagation="true"><span class="oi oi-trash" aria-hidden="true"></span></a></td>
                        </tr>
                        }
                </tbody>
            </table>
        </HayRegistros>
    </ListadoGenerico>
}

@code {
    [Parameter] public List<PersonaDTOmin> Listado { get; set; }

    private async Task AddItem()
    {

    }

    private void Eliminar(PersonaDTOmin item)
    {

    }

    private void EditarDetalle(PersonaDTOmin item)
    {

    }
}