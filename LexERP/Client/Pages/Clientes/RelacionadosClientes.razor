<h4>Clientes relacionados</h4>

@if (Listado == null)
{
    <Cargando />
}
else
{
    <button class="btn btn-primary" @onclick="@(()=>AddItem())"><span class="oi oi-plus" aria-hidden="true"></span></button>

    <ListadoGenerico Listado="Listado">
        <NoHayRegistros>

        </NoHayRegistros>
        <HayRegistros>

        </HayRegistros>
    </ListadoGenerico>
}

@code {
    [Parameter] public List<ClienteDTOmin> Listado { get; set; }

    private async Task AddItem()
    {

    }
}
