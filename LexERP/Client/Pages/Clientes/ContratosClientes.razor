@if (Listado == null)
{
    <Cargando />
}
else
{
    <hr />
    <h5>
        <span class="oi oi-document" aria-hidden="true"></span>
        Contratos
        &nbsp; <button class="btn btn-primary" @onclick="@(() => AddItem())"><span class="oi oi-plus" aria-hidden="true"></span></button>
    </h5>

    <ListadoGenerico Listado="Listado">
        <NoHayRegistros>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Tipo contrato</th>
                        <th>Descripción</th>
                    </tr>
                </thead>
            </table>
            <br />
            <text>No hay registros para mostrar</text>
        </NoHayRegistros>
        <HayRegistros>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Tipo contrato</th>
                        <th>Descripción</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Listado)
                        {
                        <tr @onclick="@(() => EditarDetalle(item))" style="cursor:pointer;">
                            <td>@item.TipoContrato</td>
                            <td>@item.Descripcion</td>
                            <td><a @onclick="@(() => Eliminar(item))" @onclick:stopPropagation="true"><span class="oi oi-trash" aria-hidden="true"></span></a></td>
                        </tr>
                        }
                </tbody>
            </table>
        </HayRegistros>
    </ListadoGenerico>
}

@code {
    [Parameter] public List<ContratoDTOmin> Listado { get; set; }

    private async Task AddItem()
    {

    }

    private void Eliminar(ContratoDTOmin item)
    {

    }

    private void EditarDetalle(ContratoDTOmin item)
    {

    }
}
